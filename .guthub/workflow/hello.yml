# name of workflow
name: hello

# manual click deliver workflow uer needs 
on:
  push:
    branch: 

# jobs:
  greet:
    runs-on: ubuntu-latest
    steps: 
      - name: say to hello user
        run: echo "Hello doston"

      - name: say hello to username
        run: echo "Hello $USER"

  details:
    runs-on: ubuntu-latest
    steps: 
      - name: checking the version python , docker , etc 
        run: docker --version && python --version
