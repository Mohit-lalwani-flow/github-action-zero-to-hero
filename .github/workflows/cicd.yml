# this is name of the workflow that will be displayed in the Actions Tab
name: CICD

# Manual click (deliver) workflow whenever user needs
on:
  workflow_dispatch:
    inputs:
      environment:
        description: This is the environmnet value where this workflow runs.
        default: 'dev'
        required: true
        type: choice
        options:
          - dev
          - prod 
          - stagging

jobs:
# every job needs seperate runner, its called parallelism
  code:
    runs-on: ubuntu-latest
    steps:
      - name: first checkout the code
        run: echo "code clone from github"
    
  build:
    needs: [code]
    runs-on: ubuntu-latest
    steps:
      - name: Build docker container
        run: echo "Docker container build .. "
    
  test:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - name: Test the container
        run: echo "Testing the container .. "

  # Here we are making input specific job 
  deploy:
    if: inputs.environment == 'dev'
    needs: [build,test]
    runs-on: ubuntu-latest
    steps:
      - name: Deployed the app
        run: echo "Docker compose deployed the app .. "
    
